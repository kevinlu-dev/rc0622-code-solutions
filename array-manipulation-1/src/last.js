/* exported last */

// define a function.
// assign a var to the last index of an array [ array.length -1 ].
// return that var.

function last(array) {
  var lastElement = array[array.length - 1];
  return lastElement;
}

// define a function.
// assign a var to the last index of an array [ array.length -1 ].
// return that var.
