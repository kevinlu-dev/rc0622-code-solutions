/* exported head */

// define a function.
// check the first element [ 0 ] of an array.
// if there is one, out put it as the return.
// if not, it will return undefined.

function head(array) {
  var firstElement = array[0];
  return firstElement;
}

// define a function.
// assign a var to the first index of an array [ 0 ].
// return that var.
